# Progetto Uni

## Struttura

- `Backend/` — API Flask
- `Frontend/` — applicazione client

## Backend (Flask)

```bash
cd Backend
python -m venv .venv
.\.venv\Scripts\activate      # Windows
pip install -r requirements.txt
python main.py                # avvia il server su http://localhost:5000
```
